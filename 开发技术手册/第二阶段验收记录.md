# AI 学习搭子第二阶段验收记录

> 阶段编号：P0-B
> 阶段名称：修复现有产品错误
> 执行日期：2026-09-09
> 当前结论：工程实现与自动验收通过；产品方人工体验待确认

---

## 一、本阶段实际完成内容

| 子任务 | 完成结果 | 状态 |
|---|---|---|
| P0-B01 学习统计 | 统计改为根据每日历史实时计算，统一使用 `Asia/Shanghai`，覆盖跨天、跨周、连续与断签 | ✅ 工程完成 |
| P0-B02 番茄钟 | 每轮生成 UUID `sessionId`；支持刷新恢复、关闭期间结束后补结算、失败重试和幂等记账 | ✅ 工程完成 |
| P0-B03 语音设置 | 陪伴页和专注页共用一个语音开关；关闭后取消旧请求、停止音频并释放资源 | ✅ 工程完成 |
| P0-B04 登录状态 | 增加后端会话校验；校验成功前隐藏受保护内容；支持网络重试和本地退出登录 | ✅ 工程完成 |
| Hydration 修复 | 浏览器能力和本地设置改为首屏安全默认值，消除已发现的首屏不一致 | ✅ 工程完成 |
| 文档同步 | README、第二阶段手册和项目级技术适配状态已更新 | ✅ 完成 |

## 二、自动验收结果

统一命令：

```bash
./scripts/verify.sh
```

| 验证层 | 结果 | 说明 |
|---|---|---|
| 后端 pytest | ✅ 62 passed | 覆盖统计、历史兼容、幂等、用户隔离、Token 有效/伪造/过期等情况 |
| 前端 ESLint | ✅ 通过 | 无阻断错误 |
| TypeScript | ✅ 通过 | 随 Next.js 正式构建完成 |
| webpack 正式构建 | ✅ 通过 | `/`、`/login`、`/focus`、`/profile` 均成功生成 |
| Playwright E2E | ✅ 13 passed | 覆盖主流程、登录、计时恢复与重试、语音跨页、无麦克风和 hydration 检查 |

浏览器用例串行执行，因为它们共用同一个临时测试账号和数据目录，避免不同用例互相修改统计。这只影响测试安排，不改变产品运行能力。

## 三、关键行为验收

| 行为 | 自动验收结果 |
|---|---|
| 连续天数、本周天数和今日统计来自每日历史 | ✅ |
| 未来日期、坏数据和负数不会污染统计 | ✅ |
| 同一用户重复提交同一个 `sessionId` 只记一次 | ✅ |
| 不同用户使用相同 `sessionId` 仍各自独立 | ✅ |
| 非 UUID `sessionId` 被接口拒绝 | ✅ |
| 计时页面刷新后按结束时间恢复 | ✅ |
| 页面关闭期间结束，重新打开会补结算且不重复 | ✅ |
| 保存失败时不制造假统计，并可沿用原编号重试 | ✅ |
| 关闭语音后陪伴页和专注页都不请求 TTS | ✅ |
| 语音开关跨页面保持一致 | ✅ |
| 没有麦克风时仍可文字聊天 | ✅ |
| 登录校验前不显示受保护内容和底部导航 | ✅ |
| 网络错误保留 Token 并允许重试 | ✅ |
| 退出登录后无法继续访问受保护页面 | ✅ |
| 自动化覆盖场景没有 hydration mismatch | ✅ |

## 四、数据与外部服务隔离

- 自动测试使用临时目录，结束后自动清理。
- 使用固定测试邀请码和仅供测试的签名密钥。
- `LLM_API_KEY` 在 E2E 中强制置空，聊天走本地回复。
- TTS 在浏览器测试中被拦截，没有调用真实语音服务。
- 未读取、修改或提交真实 `.env`、真实用户数据和线上服务。
- 旧 `stats.json` 没有删除，新统计不再依赖或写入它。

## 五、与手册预估的实现差异

| 预估项 | 实际处理 | 原因 |
|---|---|---|
| 可能修改 `backend/app/services/auth.py` | 未修改业务实现，只新增鉴权测试 | 现有 Token 验证已满足本阶段要求，无需为改而改 |
| E2E 默认多文件并行 | 固定为单 worker 串行 | 测试共用同一临时账号和统计文件，串行结果更稳定、可重复 |
| 第一阶段产品方人工体验 | 尚未登记完成 | 用户已明确授权进入第二阶段；该项继续保留，不伪装为已验收 |

## 六、已知边界与后续事项

- 当前 JSON 更新锁只保证单个后端进程内的安全写入，不能承诺多实例并发一致；P0-C 迁移数据库后解决。
- 当前退出登录只清除浏览器 Token，不支持服务端撤销；P0-C 完善账号与会话体系。
- 真实 LLM、真实 TTS 和生产部署没有执行，也不属于本阶段自动验收。
- 第二阶段没有引入数据库、账号重构、Agent Runtime、工具系统或长期记忆。

## 七、产品方人工验收待办

- [ ] 使用测试邀请码体验登录，确认校验过程中没有闪现主页面。
- [ ] 启动一轮专注并刷新，确认剩余时间符合直觉。
- [ ] 按测试方式模拟结算失败，确认能看到重试并最终只增加一次。
- [ ] 在陪伴页关闭语音后进入专注页，确认页面保持静音。
- [ ] 关闭或拒绝麦克风权限，确认仍可打字聊天。
- [ ] 在“我的”页退出，再访问首页、专注页和个人页，确认回到登录页。
- [ ] 检查页面样式、搭子图片和整体体验符合预期。

## 八、阶段结论

P0-B 的代码、自动测试和文档交付物已完成，可以视为“工程交付完成”。在产品方完成上方人工体验并确认前，本记录不把第二阶段标记为最终验收完成，也不进入 P0-C 或 Agent Harness 开发。
