# AI 学习搭子第三阶段验收记录

> 阶段：P0-C「账号与数据可靠化」
> 记录日期：2026-09-09
> 分支：`phase/p0-c-data-reliability`
> 当前结论：第三阶段工程开发已完成并由用户确认；生产发布前仍需补 PostgreSQL 实库验收

## 一、已经完成

| 项目 | 结果 | 说明 |
|---|---|---|
| 数据库结构 | 通过 | 已建立 SQLAlchemy 模型和首个 Alembic 版本 |
| 空库升级 | 通过 | 临时 SQLite 可从空库执行 `upgrade head` |
| SQLite 回滚演练 | 通过 | 临时空库已完成 upgrade → downgrade base → upgrade head |
| 数据访问层 | 通过 | 用户、邀请码、会话、学习、目标和迁移记录均有 Repository |
| 账号注册与登录 | 通过 | 邀请码只用于注册；之后使用用户名和密码登录 |
| 密码与 Token | 通过 | 密码使用 Argon2；数据库只存随机 Token 摘要 |
| 服务端退出 | 通过 | 退出后原 Token 立即失效 |
| 学习数据切换 | 通过 | 统计、历史、目标和番茄结算均使用数据库 |
| 番茄幂等 | 通过 | 数据库唯一约束保证同一用户的相同 `sessionId` 只记一次 |
| JSON 迁移工具 | 通过 | 支持 `scan`、`dry-run`、`import`、`verify`，默认不写入 |
| 邀请码管理 | 通过 | CLI 支持创建、停用和脱敏汇总 |
| 前端适配 | 通过 | 已增加账号登录、邀请码注册和服务端退出流程 |
| 自动测试 | 通过 | 后端 79 项、前端 lint/build、浏览器 14 项均通过 |

## 二、数据安全记录

| 检查项 | 结果 |
|---|---|
| 当前 `backend/data` 中的 JSON 文件数 | 0 |
| 是否读取或迁移真实用户数据 | 否 |
| 是否连接生产数据库 | 否 |
| 是否删除旧 JSON | 否 |
| 是否执行生产部署 | 否 |
| 是否把密码、Token 或邀请码明文写入数据库 | 否 |

迁移工具测试只使用自动创建的匿名临时样本。损坏样本会报告失败，源文件保持不变；同一份样本重复导入会跳过，不产生重复数据。

## 三、生产发布前仍需完成

| 待办 | 原因 | 完成条件 |
|---|---|---|
| PostgreSQL 集成测试 | 当前机器没有可用的 PostgreSQL 或 Docker 环境 | 在隔离测试库执行升级、降级、并发和 API 测试 |
| 生产迁移与发布 | 本阶段未获授权，且当前没有真实 JSON | 另行审批、备份、dry-run、import、verify 后执行 |
| 第二阶段人工验收补记 | 历史阶段尚未单独登记 | 产品方补充确认结果 |

## 四、当前判定

用户已于 2026-09-09 明确确认第三阶段开发完成，因此 P0-C 工程成果可以提交并作为第四阶段的代码基线。该确认不等于授权生产迁移或部署；在生产发布前仍必须完成 PostgreSQL 实库验收，并另行确认备份、迁移和发布操作。
