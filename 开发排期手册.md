# AI 学习搭子开发排期手册

> 文档状态：待评审  
> 排期方式：只按照需求优先级和前后依赖排序，不包含开发时间和具体负责人  
> 配套文档：《项目改进迭代方案.md》  
> 当前定位：产品已具备可演示的 AI 陪伴 MVP，后续按照“先稳定、再可靠、再 Agent 化、最后增长”的顺序推进。

---

## 一、这份手册怎么使用

本手册是开发任务的唯一排期入口。开发时从上往下推进，同一优先级内也应尽量按照任务编号顺序完成。

每项需求只有满足以下条件，才能进入下一状态：

```text
待开发 → 开发中 → 待验收 → 已完成
```

不得仅以“代码写完”作为完成标准。对应自动测试、人工验收和文档更新全部完成后，任务才算结束。

---

## 二、优先级说明

| 优先级 | 含义 | 处理原则 |
|---|---|---|
| P0 | 阻碍可靠使用或可能造成数据、安全问题 | 必须最先完成；未完成前不扩大用户范围 |
| P1 | 成为最小 Agent Harness 的核心能力 | P0 全部通过后依次实施 |
| P2 | 提升陪伴体验、留存和运营能力 | P1 主链路稳定后实施 |
| P3 | 体验增强和长期候选功能 | 根据真实用户反馈决定是否开发 |

---

## 三、总体开发顺序

```text
P0-A  建立可信开发基线
  ↓
P0-B  修复现有产品错误
  ↓
P0-C  账号与数据可靠化
  ↓
P1-A  建立 Agent 基础层
  ↓
P1-B  打通 Agent 学习闭环
  ↓
P1-C  增加记忆、安全和可观测性
  ↓
P2    主动陪伴与产品运营
  ↓
P3    长期体验增强
```

依赖原则：

- 数据库没有稳定前，不开发长期记忆。
- 用户身份没有独立前，不保存个人长期偏好。
- 工具、权限和动作协议没有完成前，不启用 Agent 自动行动。
- 核心统计不准确前，不建设周报、成就和运营指标。
- 安全规则没有完成前，不扩大情绪陪伴场景。

---

## 四、P0-A：建立可信开发基线

### P0-A01 明确唯一正式版本

需求内容：

- 将 `study-buddy/index.html` 标记为最早期单文件原型。
- 将 `backend/app/static/index.html` 标记为后端内嵌旧版 H5。
- 将 `study-buddy.md` 标记为历史 PRD 推导材料。
- 明确 `frontend/` 是唯一正式前端，`backend/` 是正式后端。
- 将上述历史文件统一移动到 `archive/` 或 `legacy/`，并确保它们不参与正式构建和测试。

前置依赖：无。

交付结果：项目中只有一套被标记为“当前正式版本”的前后端。

验收标准：

- README 不再引导用户启动旧版 H5。
- 新开发人员能够从 README 准确找到正式前端和后端。
- 历史版本不会参与正式构建和测试。

### P0-A02 修复配置模板

需求内容：

- 在 `backend/.env.example` 中补充 `INVITE_CODES`、`AUTH_SECRET`、`DATA_DIR`、时区和数据库配置。
- 所有示例值使用假数据，不放入真实密钥和生产邀请码。
- 启动时检查关键配置，生产环境禁止使用默认 `AUTH_SECRET`。

前置依赖：P0-A01。

交付结果：复制配置模板后，可以按说明启动本地系统。

验收标准：

- 无模型 Key 时可以使用本地话术。
- 有测试邀请码时可以正常登录。
- 生产环境使用默认密钥时拒绝启动或给出明确错误。

### P0-A03 建立统一测试入口

需求内容：

- 后端、前端 lint、前端生产构建和 Playwright 使用统一检查命令。
- Playwright 使用专用测试邀请码，不再硬编码或依赖本地 `.env`。
- 生产构建暂时明确使用 webpack，避免当前 Turbopack 环境问题。
- 测试数据写入独立临时目录，测试结束后不污染真实数据。

前置依赖：P0-A02。

交付结果：一条统一命令可以判断项目是否适合发布。

验收标准：

- 后端测试全部通过。
- 前端 lint 和生产构建通过。
- 登录、聊天、开始专注、查看统计的 E2E 全部通过。

---

## 五、P0-B：修复现有产品错误

### P0-B01 修复学习统计

需求内容：

- 本周专注天数从每日历史记录计算。
- 连续学习天数从每日历史记录计算。
- 统一使用用户时区；MVP 默认 `Asia/Shanghai`。
- 不再依靠容易跨天重置的统计快照计算长期指标。

前置依赖：P0-A03。

交付结果：本周专注和连续天数计算可信。

验收标准：

- 连续三天完成学习，连续天数显示 3。
- 同一周两天完成学习，本周专注显示 2 天。
- 中断一天后重新学习，连续天数从 1 开始。
- 跨周后只计算新一周的专注天数。

### P0-B02 修复番茄钟恢复与结算

需求内容：

- 每轮专注生成唯一 `sessionId`。
- 保存开始时间、结束时间、模式和运行状态。
- 页面关闭期间倒计时结束，重新打开后可以正确补结算。
- 完成接口根据 `sessionId` 防止重复记账。
- 保存失败时显示重试入口，不在前端制造假成功数据。

前置依赖：P0-A03。

交付结果：番茄钟不会漏记、重复记或假装保存成功。

验收标准：

- 运行中刷新页面，剩余时间正确。
- 关闭页面超过结束时间后再打开，只补记一次。
- 连续刷新不会重复增加番茄数。
- 后端失败时页面明确提示保存失败。

### P0-B03 统一语音设置

需求内容：

- 陪伴页和专注页共用同一份语音开关。
- 关闭语音时，取消或忽略尚未完成的 TTS 请求。
- 音频播放结束、失败、被打断或过期时释放资源。
- 麦克风不可用或权限被拒时，明确提示用户使用文字输入。

前置依赖：P0-A03。

交付结果：语音开关在所有页面表现一致。

验收标准：

- 关闭语音后，任何页面都不再播放语音。
- 快速连续发送消息时，不会播放过期回复。
- 麦克风不可用不影响文字聊天。

### P0-B04 修复登录页面状态

需求内容：

- 页面加载时先确认登录状态，再展示受保护内容。
- 无 Token、Token 过期、Token 无效时统一跳转登录页。
- 增加退出登录入口。
- 页面不能只凭本地“存在一个 Token”就判断登录有效。

前置依赖：P0-A02。

交付结果：登录状态清晰，不短暂显示不应出现的页面。

验收标准：

- 未登录不能看到主页面和底部导航。
- 过期 Token 会被清除并跳转登录。
- 用户可以主动退出登录。

---

## 六、P0-C：账号与数据可靠化

### P0-C01 建立数据库结构

需求内容：

- 本地开发使用 SQLite。
- 正式线上使用 PostgreSQL 或等价的可靠数据库。
- 建立 `users`、`invite_codes`、`study_sessions`、`daily_goals`、`chat_sessions`、`chat_messages`、`user_preferences`、`agent_events` 等基础表。
- 数据库操作与 API 业务逻辑分层。

前置依赖：P0-B01、P0-B02。

交付结果：学习数据不再依赖 serverless 本地 JSON 文件。

验收标准：

- 服务重启后数据仍然存在。
- 两个服务实例能读取同一份数据。
- 同时完成两轮学习不会互相覆盖。

### P0-C02 迁移现有 JSON 数据

需求内容：

- 迁移前备份现有数据。
- 编写只执行一次的导入程序。
- 将历史、统计和目标导入新数据库。
- 迁移过程记录成功、跳过和失败数量。
- 迁移失败可以回滚，不删除原始 JSON。

前置依赖：P0-C01。

交付结果：旧用户历史可以继续使用。

验收标准：

- 迁移前后用户数、番茄总数和总时长一致。
- 重复执行迁移不会重复写入数据。
- 数据核对通过后才切换正式读取来源。

### P0-C03 将邀请码与用户账号分离

需求内容：

- 邀请码只用于允许注册，不再直接作为用户身份。
- 每个注册用户生成独立用户 ID。
- 邀请码支持有效期、使用次数和停用状态。
- Token 支持过期、退出和撤销。
- 生产邀请码不写入公开 README。

前置依赖：P0-C01。

交付结果：多人使用同一个邀请码时仍拥有独立数据。

验收标准：

- 两个人使用同一邀请码注册后得到不同用户 ID。
- 两个账号互相看不到对方数据。
- 停用邀请码不能继续注册。
- 已撤销 Token 无法继续访问接口。

### P0-C04 完成数据切换和回滚机制

需求内容：

- API 全部改为从数据库读写。
- 保留短期只读验证方式，对比 JSON 和数据库结果。
- 准备数据库故障时的回滚和恢复说明。
- 确认后停止向旧 JSON 写入新数据。

前置依赖：P0-C02、P0-C03。

交付结果：数据库成为唯一可信数据来源。

验收标准：

- 所有统计、历史、目标和用户接口使用数据库。
- 线上重启、重新部署后数据不丢失。
- 回滚流程经过至少一次演练。

### P0 阶段发布门槛

以下条件全部满足后，才能进入 P1：

- 唯一正式前端已经明确。
- 配置模板和 README 与实际项目一致。
- 核心自动测试全部通过。
- 学习统计准确。
- 番茄钟不会重复或漏记。
- 不同用户数据完全隔离。
- 线上数据不依赖本地 JSON 文件。
- 生产环境不使用默认密钥和公开邀请码。

---

## 七、P1-A：建立 Agent 基础层

### P1-A01 定义 Agent 输入和输出协议

需求内容：

- 定义每轮 Agent 可以接收哪些信息。
- 定义普通文字回复、工具调用和前端动作的统一格式。
- 所有输出通过后端结构校验，不直接信任模型生成的 JSON。

建议输入：

- 用户最新消息。
- 当前会话摘要。
- 今日目标。
- 当前番茄钟状态。
- 今日和本周统计。
- 必要的用户偏好。

建议输出：

```json
{
  "reply": "我们先专注 25 分钟吗？",
  "actions": [
    {
      "type": "confirm_start_focus",
      "durationMinutes": 25
    }
  ]
}
```

前置依赖：P0 全部完成。

交付结果：前后端对 Agent 的输入、回复和动作理解一致。

验收标准：模型输出缺字段、字段错误或未知动作时，系统能够拒绝并安全降级。

### P1-A02 建立 Agent 工具注册表

第一批工具：

| 工具 | 用途 | 权限 |
|---|---|---|
| `get_today_goal` | 查看今日目标 | 自动允许 |
| `set_today_goal` | 保存今日目标 | 用户明确提出后允许 |
| `get_focus_state` | 查看当前专注状态 | 自动允许 |
| `request_start_focus` | 请求前端开始专注 | 必须由用户确认 |
| `request_pause_focus` | 请求暂停专注 | 用户明确提出后允许 |
| `complete_focus` | 结算有效专注 | 必须校验 `sessionId` |
| `get_study_stats` | 查看学习统计 | 自动允许 |
| `update_preference` | 修改搭子或语音偏好 | 用户明确提出后允许 |

需求内容：

- 每个工具具有名称、用途、参数、返回结果、副作用和权限说明。
- 工具通过统一注册表执行。
- 未注册工具永远不能执行。
- 工具异常统一返回给 Agent，不直接导致接口崩溃。

前置依赖：P1-A01。

交付结果：现有业务能力成为 AI 可以安全调用的工具。

验收标准：每个工具都有参数错误、正常执行、权限拒绝和执行失败测试。

### P1-A03 建立状态观察器

需求内容：

- 从数据库和当前请求中组装最小必要学习状态。
- 不把全部聊天历史和敏感账号信息发送给模型。
- 观察信息具有稳定的数据结构和长度限制。

前置依赖：P1-A01、P1-A02。

交付结果：AI 能知道用户当前目标、计时和学习进度。

验收标准：用户说“开始吧”时，AI 可以根据前文目标和当前状态理解具体含义。

### P1-A04 建立 Agent 工具权限

需求内容：

- 建立自动允许、需要确认、直接拒绝三类权限。
- 开始专注、修改目标和偏好必须符合用户当前意图。
- AI 不能直接修改番茄数和历史记录。
- 所有工具调用记录工具名、结果和权限判断，不记录密钥。

前置依赖：P1-A02。

交付结果：AI 的行动范围由程序控制，而不是只依赖 Prompt。

验收标准：伪造工具名、非法参数、重复结算和越权用户访问全部被拒绝。

---

## 八、P1-B：打通 Agent 学习闭环

### P1-B01 实现有限 Agent 循环

需求内容：

- Agent 可以执行“判断—调用工具—读取结果—继续判断”。
- 每次请求设置最大轮数、最大工具数量和总超时。
- 模型不再调用工具时结束本轮。
- 达到限制、模型失败或工具失败时安全结束并返回可理解提示。

前置依赖：P1-A03、P1-A04。

交付结果：AI 不再只能返回一句话，可以根据工具结果继续处理。

验收标准：

- “把今天目标设为背 50 个单词”能调用目标工具并确认结果。
- “我今天学了多久”能读取统计后回答。
- Agent 不会无限循环。
- 工具失败时能解释失败并保留重试入口。

### P1-B02 实现前端动作处理器

需求内容：

- 前端只识别预先定义的白名单动作。
- 开始专注等动作先显示用户确认按钮。
- 用户确认或拒绝结果发送回后端。
- 未知动作不执行，并记录错误。

前置依赖：P1-A01、P1-B01。

交付结果：AI 可以提出操作，用户保留最终决定权。

验收标准：

- AI 提议专注时显示确认入口。
- 用户拒绝后不会启动计时。
- 模型伪造未知动作时前端不会执行。

### P1-B03 将今日目标接入正式前端

需求内容：

- 提供查看、设置和修改今日目标的入口。
- 目标按用户和日期保存。
- 聊天和专注页面都能读取同一目标。
- 完成专注后，AI 可以结合目标和结果进行总结。

前置依赖：P1-B01、P1-B02。

交付结果：“定目标—开始专注—完成—总结”形成真实闭环。

验收标准：

- 用户通过输入框或聊天都能设置目标。
- 第二天不会错误沿用昨天的“今日目标”。
- AI 能正确说明目标和完成情况。

---

## 九、P1-C：记忆、安全与可观测性

### P1-C01 增加会话内记忆

需求内容：

- 保存聊天会话和最近消息。
- 每次只向模型发送必要的最近消息和摘要。
- 对话过长时生成摘要，限制上下文大小。
- 用户可以开始新会话。

前置依赖：P1-B01。

交付结果：AI 能连续理解前后文。

验收标准：连续三轮追问不会失去主题；刷新页面后当前会话可以恢复。

### P1-C02 增加受控长期记忆

需求内容：

- 只保存稳定学习偏好和用户明确要求记住的信息。
- 不自动长期保存临时情绪和敏感隐私。
- 用户可以查看、修改和删除记忆。
- 冲突时以用户最新明确指令为准。

前置依赖：P0-C04、P1-C01。

交付结果：搭子可以逐渐了解用户，但用户拥有控制权。

验收标准：偏好跨会话有效；用户删除后不再进入 Agent 上下文。

### P1-C03 增加情绪安全规则

需求内容：

- 区分普通疲惫、强烈焦虑和明显危机表达。
- 高风险内容使用固定安全流程，不继续普通口号式鼓励。
- 明确搭子不是医生，不做诊断。
- 提供联系可信人员或当地紧急帮助的引导。

前置依赖：P1-B01。

交付结果：情绪陪伴具有基本安全底线。

验收标准：高风险测试语句全部进入安全流程；模型不能绕过固定规则。

### P1-C04 增加结构化日志和 Agent 轨迹

需求内容：

- 记录请求耗时、模型来源、工具调用、权限判断、错误和降级。
- 统计 LLM、TTS、数据库和 Agent 工具成功率。
- 日志不记录 API Key、Token、邀请码和完整敏感内容。
- 为每次 Agent 请求生成可追踪 ID。

前置依赖：P1-B01。

交付结果：线上问题可以定位，Agent 行动可以审查。

验收标准：从请求 ID 可以查到完整但已脱敏的执行过程。

### P1 阶段发布门槛

- Agent 输入输出协议稳定。
- 第一批工具全部具有权限和测试。
- Agent 有轮数、工具数和超时限制。
- 用户确认机制不能被模型绕过。
- 目标、专注、完成和总结闭环通过 E2E。
- 连续对话可以保持上下文。
- 情绪安全测试通过。
- Agent 调用日志可以追踪且不泄露敏感信息。

---

## 十、P2：主动陪伴与产品运营

### P2-01 建立主动陪伴规则

需求内容：

- 专注开始、过半、完成等事件由确定性规则触发。
- AI 只负责根据上下文生成自然表达。
- 用户可以关闭中途鼓励和主动提醒。
- 不允许模型自行无限决定何时打扰用户。

前置依赖：P1 全部完成。

### P2-02 增加产品埋点

优先采集：

- 每周专注天数。
- 首次完成专注比例。
- 番茄开始到完成转化率。
- 7 日留存率。
- AI 对话后继续学习比例。
- 本地话术降级比例。
- 语音和主动提醒关闭比例。

前置依赖：P0-B01、P1-C04。

### P2-03 增加学习总结

需求内容：

- 根据真实学习记录生成每日或每周总结。
- 统计数据由程序计算，AI 只负责组织语言。
- 数据不足时不编造结论。

前置依赖：P2-02。

### P2-04 完善隐私与用户数据管理

需求内容：

- 明确文字、ASR、LLM 和 TTS 会经过哪些服务。
- 提供学习数据和记忆的查看、导出和删除入口。
- 提供账号注销流程。

前置依赖：P0-C04、P1-C02。

### P2 阶段发布门槛

- 主动陪伴可被用户控制。
- 北极星指标可以准确采集。
- 学习总结只使用真实数据。
- 用户可以管理和删除自己的数据。

---

## 十一、P3：长期候选功能

以下功能不直接进入开发，先根据 P2 数据决定：

| 候选功能 | 开发前必须满足的条件 |
|---|---|
| 成就徽章 | 真实用户确实需要额外激励，且不会造成打卡压力 |
| 自定义专注时长 | 默认 25 分钟无法覆盖主要用户习惯 |
| 学习提醒 | 用户主动订阅，且具备可靠通知渠道 |
| 搭子播报周报 | 周报打开率和语音使用率证明有价值 |
| 更丰富的搭子形象 | 留存问题确实与视觉陪伴感相关 |
| Live2D 或数字人 | 已验证静态形象限制留存，且成本可接受 |
| 多人自习室 | 单人陪伴核心闭环已经稳定，并有明确多人需求 |

---

## 十二、需求总排期表

| 顺序 | 编号 | 需求 | 优先级 | 前置依赖 | 完成标志 |
|---:|---|---|---|---|---|
| 1 | P0-A01 | 明确唯一正式版本 | P0 | 无 | 新人不会进入旧前端 |
| 2 | P0-A02 | 修复配置模板 | P0 | P0-A01 | 本地配置可复制使用 |
| 3 | P0-A03 | 建立统一测试入口 | P0 | P0-A02 | 核心检查一键通过 |
| 4 | P0-B01 | 修复学习统计 | P0 | P0-A03 | 跨天跨周结果准确 |
| 5 | P0-B02 | 修复番茄钟恢复与结算 | P0 | P0-A03 | 不漏记、不重复记 |
| 6 | P0-B03 | 统一语音设置 | P0 | P0-A03 | 所有页面遵守语音开关 |
| 7 | P0-B04 | 修复登录页面状态 | P0 | P0-A02 | 登录、过期、退出行为正确 |
| 8 | P0-C01 | 建立数据库结构 | P0 | P0-B01、P0-B02 | 数据库可稳定读写 |
| 9 | P0-C02 | 迁移现有 JSON 数据 | P0 | P0-C01 | 迁移数据核对一致 |
| 10 | P0-C03 | 邀请码与账号分离 | P0 | P0-C01 | 每个用户有独立身份 |
| 11 | P0-C04 | 完成数据切换与回滚 | P0 | P0-C02、P0-C03 | 数据库成为唯一事实来源 |
| 12 | P1-A01 | 定义 Agent 输入输出协议 | P1 | P0 全部完成 | 协议有校验和降级 |
| 13 | P1-A02 | 建立 Agent 工具注册表 | P1 | P1-A01 | 第一批工具可统一执行 |
| 14 | P1-A03 | 建立状态观察器 | P1 | P1-A01、P1-A02 | AI 能看到必要学习状态 |
| 15 | P1-A04 | 建立 Agent 工具权限 | P1 | P1-A02 | 敏感动作受到程序控制 |
| 16 | P1-B01 | 实现有限 Agent 循环 | P1 | P1-A03、P1-A04 | AI 能行动、观察并安全停止 |
| 17 | P1-B02 | 实现前端动作处理器 | P1 | P1-A01、P1-B01 | AI 动作必须经过白名单和确认 |
| 18 | P1-B03 | 今日目标接入正式前端 | P1 | P1-B01、P1-B02 | 目标到总结闭环完成 |
| 19 | P1-C01 | 增加会话内记忆 | P1 | P1-B01 | 连续聊天不丢失主题 |
| 20 | P1-C02 | 增加受控长期记忆 | P1 | P0-C04、P1-C01 | 偏好可记忆、可删除 |
| 21 | P1-C03 | 增加情绪安全规则 | P1 | P1-B01 | 高风险内容进入安全流程 |
| 22 | P1-C04 | 增加日志和 Agent 轨迹 | P1 | P1-B01 | 请求过程可追踪且脱敏 |
| 23 | P2-01 | 建立主动陪伴规则 | P2 | P1 全部完成 | 主动提醒可控、不扰民 |
| 24 | P2-02 | 增加产品埋点 | P2 | P0-B01、P1-C04 | 北极星指标可采集 |
| 25 | P2-03 | 增加学习总结 | P2 | P2-02 | 总结基于真实数据 |
| 26 | P2-04 | 完善隐私与数据管理 | P2 | P0-C04、P1-C02 | 用户可查看、导出、删除数据 |
| 27 | P3 | 长期候选功能 | P3 | P2 数据验证 | 根据真实需求单独立项 |

---

## 十三、统一完成标准

任意需求标记为“已完成”前，必须同时满足：

1. 功能实现与需求描述一致。
2. 相关单元测试、接口测试或前端测试通过。
3. 不影响现有登录、聊天、专注和统计主流程。
4. 错误时有明确提示或安全降级方式。
5. 涉及数据变化时具有备份、幂等或回滚方案。
6. 涉及 Agent 动作时具有权限判断和用户确认。
7. 不在代码、日志和文档中泄露密钥、Token、邀请码和敏感数据。
8. README、接口说明和产品文档已经同步更新。
9. 完成一次对应的人工用户流程验收。

---

## 十四、最终执行原则

本项目不需要推倒重做。现有前端、后端、聊天、语音和番茄钟可以继续使用。

整个排期坚持三条原则：

1. **先正确，再智能**：数据和统计不可靠时，不增加 AI 自动操作。
2. **先受控，再自主**：Agent 每个动作都经过白名单、权限和必要的用户确认。
3. **先验证，再扩展**：P3 功能必须由真实用户反馈和产品数据驱动。

最终升级路径为：

```text
可靠的学习陪伴应用
→ 具有工具、观察和记忆的最小 Agent Harness
→ 可持续优化的 AI 学习陪伴产品
```
